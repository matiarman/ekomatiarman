name: Deploy Vulnerable Application                                  # workflow name

on:
  push:
    branches:
      - master
jobs:
  deploy-heroku:
    name: Deploy Heroku
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.2dc24c54-5f0d-4c0d-8010-309225b52f4f}}
          heroku_app_name: "ekomatiarman" #Must be unique in Heroku
          heroku_email: "matiarman@gmail.com"
          remote_branch: "master"
          usedocker: true
